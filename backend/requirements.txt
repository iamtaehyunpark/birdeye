fastapi>=0.109.0
uvicorn[standard]>=0.27.0
opencv-python-headless>=4.9.0
numpy>=1.24.0
ultralytics>=8.1.0
filterpy>=1.4.5
Pillow>=10.0.0
python-multipart>=0.0.9
scipy>=1.12.0
python-dotenv>=1.0.0
aiofiles>=23.0.0

# GPU-accelerated feature matching (recommended)
# Requires PyTorch — install the CUDA version matching your driver first:
#   https://pytorch.org/get-started/locally/
# Then install LightGlue:
#   pip install git+https://github.com/cvg/LightGlue.git
#
# CPU-only PyTorch (slower but functional):
torch>=2.2.0
torchvision>=0.17.0
